<%= form_for(@auxilio) do |f| %>
  <% if @auxilio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auxilio.errors.count, "error") %> prohibited this auxilio from being saved:</h2>

      <ul>
      <% @auxilio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :logo %><br />
    <%= f.text_field :logo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
